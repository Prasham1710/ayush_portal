/** @type {import('next').NextConfig} */
const nextConfig = {
     images: {
        experimental: {
        appDir: true,
    },
        domains: [
            'lh3.googleusercontent.com'
        ]
    }}

module.exports = nextConfig
